// Re-exported from the consolidated key-builder module. Kept for backward
// compatibility with existing imports (including test files, which are not
// modified as part of internal refactors).
export { seatHoldKey } from './redis.keys';
